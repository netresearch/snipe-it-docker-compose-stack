# SPDX-License-Identifier: MIT
# Copyright (c) 2026 Netresearch DTT GmbH
#
# Dependabot configuration.
#
# Renovate (renovate.json) is the primary dependency manager for this repo:
# - Docker base images, PHP packages, Snipe-IT version pins.
#
# Dependabot covers GitHub Actions specifically as a belt-and-suspenders
# fallback against Renovate downtime / outage (Netresearch org convention).
# Docker is also included on a monthly cadence so a Renovate outage longer
# than a week still produces image-update PRs.
#
# Reference: https://docs.github.com/code-security/dependabot/dependabot-version-updates/configuration-options-for-the-dependabot.yml-file
---
version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "06:00"
      timezone: "Europe/Berlin"
    open-pull-requests-limit: 10
    commit-message:
      prefix: "ci"
    labels:
      - "dependencies"
      - "github-actions"
    # Group minor/patch into one PR per week. Major bumps come as
    # individual PRs (one per action) so each can be reviewed against
    # its upstream CHANGELOG — they typically carry breaking changes
    # but are usually small per-action.
    groups:
      actions-minor-and-patch:
        update-types:
          - "minor"
          - "patch"

  - package-ecosystem: "docker"
    directory: "/"
    schedule:
      interval: "monthly"
      day: "monday"
      time: "06:00"
      timezone: "Europe/Berlin"
    open-pull-requests-limit: 5
    commit-message:
      prefix: "docker"
    labels:
      - "dependencies"
      - "docker"
